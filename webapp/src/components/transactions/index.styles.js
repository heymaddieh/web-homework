import {css} from '@emotion/core';
import {colors} from '../../global';

export default {
  container: css({
    width: '100%',
    border: `1px solid ${colors.grey}`,
    padding: '10px',
    borderRadius: '4px'
  }),
}